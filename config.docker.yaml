# Lobby Docker 配置
server:
  host: "0.0.0.0"
  httpPort: 9001
  mqPort: 9002

nats:
  url: "nats://nats:4222"

log:
  level: "info"
  logFile: "lobby"
  maxSize: 500
  maxAge: 30

mongo:
  uri: "mongodb://mongo:27017"
  database: "lobby"

redis:
  host: "redis"
  port: 6379
  password: ""
  db: 0
