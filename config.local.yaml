# Lobby 本地开发配置（连接中间件容器）
server:
  host: "0.0.0.0"
  httpPort: 9001
  mqPort: 9002

nats:
  url: "nats://nats:4222"

log:
  level: "info"
  logFile: "lobby"
  errorFile: "lobby_error"
  logDir: "logs"
  maxSize: 500
  maxAge: 30

mongo:
  uri: "mongodb://mongo:27017"
  database: "lobby"

redis:
  host: "redis"
  port: 6379
  password: ""
  db: 0

elect:
  mode: "etcd"
  prefix: "/elect/leader"
  ttl: 10
  etcd:
    endpoints:
      - "etcd:2379"
    dialTimeout: 5000
